@page "/"
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        // Redirect to Railcar Trips page on load
        Navigation.NavigateTo("/railcar-trips", replace: true);
    }
}
